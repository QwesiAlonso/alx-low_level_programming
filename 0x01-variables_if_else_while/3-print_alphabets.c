#include <stdio.h>
/**
 * main - Entry point
 * Return: Outcome 0 (Success)
 */

int main(void)
{
	char m;

	char n;

	m = 'a';
	n = 'A';
	while
		(m <= 'z') {
			putchar(m);
			m + = 1;
		}
	while
		(n <= 'z') {
			putchar(n);
			n + = 1;
		}
	putchar('\n');
	return (0);
}
