#include <stdio.h>
/**
 * main - Enter point
 * Return: Outcome 0
 */
int main(void)
{
	int m;
	int n;

	for (m = 48; m <= 57; m++)
	{
		putchar(m);
	}
	for (n = 97; n <= 102; n++)
	{
		putchar(n);
	}
	putchar('\n');
	return (0);
}
